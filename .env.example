# Copie este arquivo para .env e ajuste as variáveis de acordo com o ambiente
PORT=3333

# URL usada pelo servidor em produção. Prefira o host do Connection Pooler do Supabase
# (aba "Connect" > "Connection string" > "Connection pooler") para suportar mais conexões.
DATABASE_URL="postgresql://postgres:Felipegestao-pesoal@aws-0-sa-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1&pool_timeout=10"

# URL direta usada para migrações (`prisma migrate deploy`). Vá na aba "Direct connection"
# e copie o host `db.<projeto>.supabase.co` na porta 5432, sempre com `?sslmode=require`.
DIRECT_URL="postgresql://postgres:Felipegestao-pesoal@db.seu-projeto.supabase.co:5432/postgres?sslmode=require"

# Ajuste os logs do Prisma conforme necessário
PRISMA_LOG_WARNINGS=true
PRISMA_LOG_QUERIES=false
